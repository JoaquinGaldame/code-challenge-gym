# Two Sum

## English
Given an array of integers `nums` and an integer `target`, return the indices of the two numbers such that they add up to `target`.

You may assume that each input has exactly one solution, and you may not use the same element twice.

Implement `solve(data)` in `python/solution.py`, where `data` is a dictionary with:
- `nums`: list of integers
- `target`: integer

Return a list with the two indices in the order they are found.

### Example
- Input: `{"nums": [2, 7, 11, 15], "target": 9}`
- Output: `[0, 1]`

## Espanol
Dado un arreglo de enteros `nums` y un entero `target`, devuelve los indices de los dos numeros cuya suma sea igual a `target`.

Puedes asumir que cada entrada tiene exactamente una solucion y que no puedes usar el mismo elemento dos veces.

Implementa `solve(data)` en `python/solution.py`, donde `data` es un diccionario con:
- `nums`: lista de enteros
- `target`: entero

Devuelve una lista con los dos indices en el orden en que se encuentran.
